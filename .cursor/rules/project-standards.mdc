---
description: Wallpaper 项目工程与提交规范
alwaysApply: true
---

# Wallpaper 工程规范

## Monorepo 约束

- 子项目目录固定为 `apps/web`、`apps/server`、`apps/desktop`。
- 新增功能时优先在对应子项目内实现，避免跨目录随意放置代码。

## 代码质量

- 所有 TypeScript 代码必须通过 ESLint 检查。
- 提交前必须执行格式化，保持 Prettier 结果一致。
- 禁止提交 `dist`、`release` 等构建产物。

## 文档规范

- 功能变更需要同步更新 `README.md` 或 `docs` 中相关文档。
- 技术决策变化必须更新 `docs/tech-stack.md`。

## Git 提交规范

- 必须使用 Angular Commit Message 规范。
- 推荐类型：`feat`、`fix`、`refactor`、`docs`、`chore`、`build`、`ci`、`test`。
- 提交信息使用中文，且描述变更目的，不只罗列文件修改。
